import os,sys
from pathlib import Path, WindowsPath
from pathlib import *
import pandas as pd
import sys
print(sys.path)          # PRINTS PYTHONPATH
# sys.path.insert(0,'path')   - path to insert in PYTHONPATH
# Add 'PYTHONPATH' variable in environment variable for user.
# python will look for path given in PYTHONPATH every time when we import module.

